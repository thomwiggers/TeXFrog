\textbf{Claim.}
  Games \tfgamename{kemdem}{G2} and \tfgamename{kemdem}{G3} are identically
  distributed.

The nonce $r$ is sampled fresh, so $\RF(r)$ has never been evaluated before and
is a uniformly random value. Replacing $\RF(r)$ with an independently sampled
uniform pad is a perfect rewrite. Again only the \emph{Key derivation} segment
changes.
