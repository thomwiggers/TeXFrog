\textbf{Claim.}
  Games \tfgamename{kemdem}{G1} and \tfgamename{kemdem}{G2} are computationally
  indistinguishable assuming $\mathrm{PRF}$ is a secure pseudorandom function.

Since $K$ is now uniformly random and used only as a PRF key, we may replace
$\mathrm{PRF}(K, \cdot)$ with a truly random function $\RF$. Only the
\emph{Key derivation} segment changes.
